<% if @from_dashboard %>
  <%= turbo_stream.replace "dashboard-index" do %>
    <%= render "pages/dashboard_content" %>
  <% end %>
<% else %>
  <%= turbo_stream.replace dom_id(@registration, :status) do %>
    <%= render "registrations/status_badge", registration: @registration %>
  <% end %>
<% end %>
